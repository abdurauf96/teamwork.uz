@extends('layouts.backend')

@section('content')
    <div class="container">
        <div class="row">
            <div class="col-md-10">
                <div class="card">
                    <div class="card-header">Proccess {{ $proccess->id }}</div>
                    <div class="card-body">

                        <a href="{{ url('/admin/proccesses') }}" title="Back"><button class="btn btn-warning btn-sm"><i class="fa fa-arrow-left" aria-hidden="true"></i> Back</button></a>
                        <a href="{{ url('/admin/proccesses/' . $proccess->id . '/edit') }}" title="Edit Proccess"><button class="btn btn-primary btn-sm"><i class="fa fa-pencil-square-o" aria-hidden="true"></i> Edit</button></a>
                        {!! Form::open([
                            'method'=>'DELETE',
                            'url' => ['admin/proccesses', $proccess->id],
                            'style' => 'display:inline'
                        ]) !!}
                            {!! Form::button('<i class="fa fa-trash-o" aria-hidden="true"></i> Delete', array(
                                    'type' => 'submit',
                                    'class' => 'btn btn-danger btn-sm',
                                    'title' => 'Delete Proccess',
                                    'onclick'=>'return confirm("Confirm delete?")'
                            ))!!}
                        {!! Form::close() !!}
                        <br/>
                        <br/>

                        <div class="table-responsive">
                            <table class="table">
                                <tbody>
                                    <tr>
                                        <th>ID</th><td>{{ $proccess->id }}</td>
                                    </tr>
                                    <tr><th> Title Uz </th><td> {{ $proccess->title_uz }} </td></tr>
                                    <tr><th> Title Ru </th><td> {{ $proccess->title_ru }} </td></tr>
                                    <tr><th> Title En </th><td> {{ $proccess->title_en }} </td></tr>
                                </tbody>
                            </table>
                        </div>

                    </div>
                </div>
            </div>
        </div>
    </div>
@endsection
