@extends('layouts.backend')

@section('content')
    <div class="container">
        <div class="row">
           

            <div class="col-md-12">
                <div class="card">
                    <div class="card-header">ServiceTechnology {{ $servicetechnology->id }}</div>
                    <div class="card-body">

                        <a href="{{ url('/admin/service-technologies') }}" title="Back"><button class="btn btn-warning btn-sm"><i class="fa fa-arrow-left" aria-hidden="true"></i> Back</button></a>
                        <a href="{{ url('/admin/service-technologies/' . $servicetechnology->id . '/edit') }}" title="Edit ServiceTechnology"><button class="btn btn-primary btn-sm"><i class="fa fa-pencil-square-o" aria-hidden="true"></i> Edit</button></a>
                        {!! Form::open([
                            'method'=>'DELETE',
                            'url' => ['admin/servicetechnologies', $servicetechnology->id],
                            'style' => 'display:inline'
                        ]) !!}
                            {!! Form::button('<i class="fa fa-trash-o" aria-hidden="true"></i> Delete', array(
                                    'type' => 'submit',
                                    'class' => 'btn btn-danger btn-sm',
                                    'title' => 'Delete ServiceTechnology',
                                    'onclick'=>'return confirm("Confirm delete?")'
                            ))!!}
                        {!! Form::close() !!}
                        <br/>
                        <br/>

                        <div class="table-responsive">
                            <table class="table">
                                <tbody>
                                    <tr>
                                        <th>ID</th><td>{{ $servicetechnology->id }}</td>
                                    </tr>
                                    <tr><th> Service </th><td> {{ $servicetechnology->service['name_'.\App::getLocale()] }} }} </td>
                                    </tr><tr><th> Image </th><td> <img src="/admin/images/{{ $servicetechnology->image }}" width="80" alt=""> </td></tr>
                                </tbody>
                            </table>
                        </div>

                    </div>
                </div>
            </div>
        </div>
    </div>
@endsection
